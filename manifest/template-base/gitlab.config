# Image Registry
imageRegistry=''

# GitLab template version
templateVersion=5.0.1

# Gitlab configs
storageSize=30Gi
serviceType=Ingress

# HyperAuth
authUrl=''    # 키클록 URL (`http://`또는 `https://` 포함)
authClient='' # 키클락 클라이언트
authSecret='' #키클락 시크릿
authTLSSecretName='' #씨크릿 이름
custom_domain_name='' #(`http://`또는 `https://`미포함)
